# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.class
*.so

# C extensions
*.o
# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!frontend/src/packages/**/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
# PyInstaller
*.manifest
*.spec
# Installer logs
pip-log.txt
pip-delete-this-directory.txt
# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
# Translations
*.mo
*.pot
# Django stuff:
*.log
logs/
local_settings.py
db.sqlite3
db.sqlite3-shm
db.sqlite3-wal
fireai_learning.sqlite3
/db/
backend/db/api_keys.json
backend/db/api_keys.secret
# Logs
# Coverage
.coverage*
# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
# Secrets
secrets/
secret.json
config/secrets.json
# API key server-side HMAC secret (per-instance, must never be committed)
db/api_keys.secret
*.secret
.env.backup.*
# Testing
.pytest_cache/
# Reports (generated)
reports/
coverage.json
audit/
# But allow phase reports in docs
!docs/reports/
# Node.js
node_modules/
# Electron
frontend/release/
frontend/electron/backend/
frontend/electron/core/
frontend/electron/fireai/
frontend/electron/parsers/
frontend/electron/requirements.txt
# Build artifacts
frontend/dist/
# Database files (M-1 FIX: also exclude root-level .db files)
*.db
*.db-shm
*.db-wal
# Audit logs
*.jsonl
skills/ppt/scripts/tectonic
# Python
# Environment Variables - NEVER COMMIT THESE
.env.*.local
# Virtual environments
.venv/
.venv312/
# IDE files
*~
.project
.settings/
# OS files
# Secrets and keys
*.key
*.pem
*.cert
*.crt
# Node modules (frontend)
frontend/node_modules/
# Downloaded files
download/
# Audit logs (optional - archive periodically)
audit/*.json
# Memory storage
mem0_qdrant_data/
# Database files
*.sqlite
# Test artifacts
coverage/
tmp/
temp/
# FireAI artifacts
fireai/.cache/
qomn_fire/integration/output/
# Backup files
*.bak
*.backup
*.old
# Generated files
*.pyd
# Git worktrees
worktree-*/
# V127 SAFETY HARDENING — additional sensitive file patterns
# Private keys (all formats)
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_dsa
id_ecdsa
id_ed25519
authorized_keys
*_rsa
*_dsa
*_ecdsa
# Cloud credentials
.aws/
.gcp/
.azure/
*.kubeconfig
# Database dump files (may contain PII / audit data)
*.db-journal
*.sql
*.dump
*.dmp
# HashiCorp Vault tokens / secrets
.vault-token
*.vault
# OS-level sensitive files
# IDE state that may leak secrets (when env vars are inline)
.vscode/settings.json
# Coverage reports may contain source paths
.ruff_cache/
.bandit/
.venv-audit/
.venv_v270/
.venv-*/
# Runtime data files (modified by backend, should not be committed
conversion_history.json
# V141.4: Runtime data directories (Mem0, Qdrant, SQLite checkpoints)
data/
# Vercel linkage folder
.vercel/
# Temporary Hugging Face deployment folder and script
hf_deploy/
deploy_to_hf.py
.vercel
.env*
# C-09 FIX: Auto-generated project index (regenerate in CI as artifact)
PROJECT_INDEX.json
# Vercel Python build cache
.vercel_python_packages/
sonar_issues.json
# Large/test output files
newman_test_results.json
postman_collection.json
postman_collection_updated.json
API_Test_Collection_Updated.postman_collection.json
BAZSPARK/
BAZSPARK-hf/
BAZSPARK_HF/
BAZspark/
revit/
.playwright-mcp/
nul
.kilo/
postman_collection*.json
*.sonarbak
# ── Cloudflare ──────────────────────────────────────────────────────────────
-wrangler/
.dev.vars
.mf/
test.ledger
quick_shots.cjs
# Revit API data files (too large for git, downloaded on demand)
revit_data/RevitAPI*.json
# Coverage reports (regenerated on each test run)
reports/coverage.json
uploads/
# Worklog files (transient agent work logs)
worklog.md
# Exception: the audit worklog is committed because backend/tests/security/
# regression-guard tests assert its claim texts (NO SKIP policy — the file
# must exist on CI for those tests to run instead of skipping).
!worklog.md
worklog_entry.md
# Stress test / benchmark result JSONs (regenerated on demand)
HTTP_STRESS_TEST_RESULTS.json
STRESS_TEST_RESULTS.json
STRICT_STRESS_V3_RESULTS.json
sonar_fix_plan.json
# .NET build artifacts (AutoCAD/Revit add-ins)
bin/
obj/
# Agent workspace directories
.mimocode/
# Agent scratch scripts (temporary, not part of codebase)
scratch/
*.json.tmp
.claude/
feature_flags.json
# AI prompt files � should never be committed to the repo
/agent.md
/agent_*.md

# Agent scratch scripts (temporary, not part of codebase)
scratch/
*.json.tmp
